{% if not card_list %}
    {% set card_list = params %}
{% endif %}
{% if card_list.items and card_list.items.length %}
  <div class="fr-grid-row fr-grid-row--gutters fr-mt-6w" data-pagefind-ignore>
    {% for item in card_list.items %}
      <div class="fr-col-12 fr-col-sm-6 fr-col-md-{{ card_list.columns or 4 }}">
        {% set card = {
          title: item.data.title,
          url: item.url,
          description: item.data.description,
          tags: item.data.tags,
          image: item.data.image,
          date: item.data.date,
          disabled: item.data.disabled,
          ratio: card_list.ratio_cards
        } %}
        {% include "components/card_dsfr.njk" %}
      </div>
    {% endfor %}
  </div>
{% endif %}